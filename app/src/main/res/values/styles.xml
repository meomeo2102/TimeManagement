<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Nút Lưu -->
    <style name="RoundedButtonStyle" parent="Widget.Material3.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:backgroundTint">@color/buttonEdit</item>
    </style>

    <!-- Nút Xóa -->
    <style name="RoundedDeleteButtonStyle" parent="Widget.Material3.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:backgroundTint">@color/buttonDelete</item>
    </style>

    <!-- Text cho BottomNavigation -->
    <style name="BottomNavTextAppearanceActive" parent="TextAppearance.Material3.LabelLarge">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="BottomNavTextAppearanceInactive" parent="TextAppearance.Material3.LabelMedium">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/textSecondary</item>
    </style>

</resources>
